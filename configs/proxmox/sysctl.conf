# ==============================================================================
# Proxmox VE Kernel Parameters
# Location: /etc/sysctl.conf
# Purpose: Optimize memory management to extend the lifespan of the primary SSD.
# ==============================================================================

# Reduce swappiness to minimize disk writes (default is usually 60)
vm.swappiness=10
